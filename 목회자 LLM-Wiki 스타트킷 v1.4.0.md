# 목회자 LLM-Wiki 스타트킷 v1.4.0

> 수집 → 원본 보존 → Wiki 재사용 → MOC 연결 → 설교 → 활용

- **릴리스**: v1.4.0 · 2026-09-10
- **범위**: 1단계(설교 중심)
- **직전 배포**: v1.3.1 (2026-09-09) — 구조는 같고, **실제로 원고 200편 가까이를 등록해 보면서 얻은 규칙**이 더해졌다
- **버전 계보**: 이 킷은 `목회자 LLM-Wiki 스타트킷` 계보다(v1.2.0 → v1.3.0 → v1.3.1 → **v1.4.0**). 볼트 폴더 이름의 `V3`은 **개인 볼트 세대**이고 킷 버전이 아니며, 별도로 배포된 `BC_LLM_WiKI v3.0.x`는 **구조가 다른 폐기된 계보**다 — 번호를 섞지 말 것
- **한 문장**: 목회자가 지금까지 설교하고 읽고 생각한 것을 쌓아 다음 설교를 돕는 개인 지식 시스템이며, **판단은 사람이 하고 AI는 연결만 한다.**

---

## 0. 이 문서를 어떻게 쓰는가

이 파일 하나만 읽으면 다른 목회자도 같은 시스템을 세울 수 있게 만들었다.

| 목적 | 읽을 곳 |
|---|---|
| 30분 안에 첫 성공 | [1장](#1-설치와-첫-30분) |
| 매주 어떻게 돌리는가 | [2장](#2-주간-운영-리듬) · [3장](#3-명령어) |
| **AI에게 넘길 규칙 전문** | [6장](#6-ai에게-주는-지침-복사해서-쓰는-부분) ← 가장 중요 |
| 규칙이 왜 이렇게 됐는가 | [7장](#7-값을-치르고-배운-것) |

> [!tip] 핵심 통찰
> 이 시스템의 값은 폴더 구조가 아니라 **7장**에 있다. 구조는 하루면 만들지만,
> 7장의 규칙들은 원고 수백 편을 실제로 처리하다 **원고를 잃고, 통계를 틀리고,
> 지어낸 인용을 걸러내면서** 얻은 것이다. 구조만 복사하고 7장을 버리면 같은 값을 다시 치른다.

---

## 1. 설치와 첫 30분

### 1-1. 볼트 만들기

1. 빈 폴더를 하나 만들고 Obsidian에서 **새 볼트로 열기**.
2. 아래 폴더를 만든다. **`00. 수집함`과 `10. 원본자료`의 하위 폴더 이름은 반드시 같아야 한다** — 파일을 옮길 때 짝이 맞아야 한다.

```text
00. 수집함/          기존자료 · 책 · 블로그 · 유튜브 · 기사와자료 · 떠오른생각
10. 원본자료/        (같은 하위 폴더 6개)
20. 설교위키/
    21. 성경본문/  22. 설교주제/  23. 성경인물/  24. 신학/
    25. 예화/      26. 인용문/    27. 묵상과통찰/  28. 설교아이디어/
    29. MOC/       (성경본문 · 설교주제 · 성경인물 · 신학)
30. 설교준비/        주석 · 1차설교 · 연결자료 · 완성설교
50. 질문/
70. 설교활용/        소그룹나눔 · 주보에세이 · 카드뉴스 · 숏츠 · SNS
80. 참고자료/
90. 설정/            명령어 · 템플릿 · 출력레시피 · 출력예시 · 사용안내
index.md             볼트 통계와 MOC 목록
log.md               작업 로그 (append-only)
```

3. **6장 전문을 `CLAUDE.md`로 저장한다** (Codex·Antigravity를 쓰면 `AGENTS.md`로도 같은 내용을 복사 — 두 파일은 항상 같아야 한다).

### 1-2. 기존자료가 뜻하는 것

`기존자료`는 설교만이 아니다. **강의·성경공부·교육자료 등 목회자가 과거에 완성한 모든 자료**를 포괄한다. 프론트매터의 `자료유형`으로 구분한다.

### 1-3. 첫 성공 — 한 편만

전체를 한꺼번에 넣지 마라. **원고 한 편**을 `10. 원본자료/기존자료`에 넣고 AI에게:

> `/등록` 방금 넣은 설교 한 편만 등록해 줘. 원본은 바이트 그대로 보존하고,
> 새로 만들 Wiki와 수정할 Wiki를 **먼저 알려준 다음** 진행해.

그리고 결과를 직접 열어 확인한다 — 원문이 그대로 있는지, 뽑아낸 Wiki가 실제로 원고에 있는 내용인지. **여기서 한 번 확인해 두면 나머지 수백 편을 믿고 맡길 수 있다.**

---

## 2. 주간 운영 리듬

| 주기 | 할 일 |
|---|---|
| 그날 | 발견한 자료를 `00. 수집함`에 넣기만 한다 (분류·정리 금지) |
| 주 1회 | `/수집`으로 수집함 비우기 |
| 설교 준비 전 | `/설교연결` — 초안을 넣고 관련 지식을 받는다 |
| 설교 확정 후 | `/설교완성` |
| 필요할 때만 | `/활용` |
| 월 1회 | `/점검` — 미검토·중복·끊긴 출처 확인 |

> [!note] 해석 제안
> 수집함에 쌓인 파일 수를 **실작업량으로 읽지 마라.** 원본 앱에서 전체를 다시 내보내면
> 이미 등록한 것이 재유입된다. 실제 경험에서 재유입분 16편 중 **11편이 중복**이었다.

---

## 3. 명령어

`/수집` 같은 것은 **AI와 약속한 요청어**다. Claude Code에서는 슬래시 명령으로, 일반 채팅에서는 그냥 그렇게 말하면 된다.

| 명령 | 하는 일 | Wiki 추출 | MOC 연결 |
|---|---|:-:|:-:|
| `/수집` | 새 자료(책·유튜브·블로그 등)를 원본 보존 후 지식 추출 | O | O |
| `/등록` | **과거에 완성한 자료**를 원본으로 등록 | O | O |
| `/주석` | 성경 본문 AI 리서치를 참고자료로 만듦 | **X** | **X** |
| `/설교연결` | 1차설교를 분석해 별도 `연결자료` 생성 (**초안은 수정 금지**) | X | X |
| `/설교완성` | 확정 설교를 보존하고 Wiki·MOC 갱신 | O | O |
| `/활용` | 완성설교를 소그룹·주보·카드뉴스·숏츠·SNS로 변환 | **X** | X |
| `/질문` | 설교와 무관한 자유 질문을 기존 지식에 묻는다 | X | X |
| `/상태` | 현황 스냅샷 — **읽기 전용** | X | X |
| `/점검` | 월간 점검 자동화 — **읽기 전용** | X | X |

세 가지가 의도된 예외다. **`/주석`은 Wiki로 쪼개지 않는다**(AI 리서치라 검증 전이다). **`/활용` 결과는 Wiki에 재수집하지 않는다**(중복이 된다). **`/설교연결`은 목회자의 초안을 절대 고치지 않는다**(제안은 별도 파일에만 쓴다).

---

## 4. 여덟 가지 Wiki — 무엇을 어디에 넣는가

한 노트는 **한 개념만** 다룬다. 이것이 재사용의 조건이다.

| 폴더 | 담는 것 | 제목 예 |
|---|---|---|
| `21. 성경본문` | 한 본문 단락의 문맥·구조·설교 질문 | `요한복음 15장 1-8절` |
| `22. 설교주제` | 반복해서 설교하는 주제 (**허브**) | `두려움` · `감사` |
| `23. 성경인물` | 인물 한 사람 (**허브**) | `바울` · `드보라` |
| `24. 신학` | 개념·원어 (**허브**) | `케노시스` · `평강 (샬롬)` |
| `25. 예화` | 이야기 하나 | `이작 펄만 - 끊어진 줄로 연주를 완성하다` |
| `26. 인용문` | 남의 문장 하나 | `힘을 달라고 기도했더니` |
| `27. 묵상과통찰` | **"A가 아니라 B" 형태의 재정의** | `거듭남은 노력이 아니라 선물이다` |
| `28. 설교아이디어` | 아직 설교가 안 된 착상 | |

> [!tip] 핵심 통찰
> **`27. 묵상과통찰`이 가장 빠르게 자라고 가장 쓸모 있다.** 실제 볼트에서 612개 노트 중
> 210개가 여기 있다. 제목을 **"A가 아니라 B"** 로 쓰면 나중에 제목만 훑어도 설교가 보인다.
> `기도는 끝이 아니라 시작이다` · `심판은 멸망이 아니라 정결의 시작이다` 처럼.

### MOC — 만들 때와 만들지 않을 때

MOC(Map of Content)는 여러 노트를 묶는 **설교 연구 지도**다.

- **만든다**: 관련 Wiki가 **5개 이상** 모였거나, 반복 등장하는 큰 질문이 생겼을 때
- **만들지 않는다**: 노트마다 하나씩 (그건 태그가 할 일이다)
- **반드시**: 배치·등록을 마칠 때마다 그때 생긴 노트를 MOC에 연결한다

> [!warning] 상충하는 정보
> MOC 연결을 미루면 지도가 죽는다. 실제로 네 배치를 미뤘더니 사도행전 MOC 하나에
> **미연결 43건**이 쌓였고, 그중 하나는 그 지도가 "가장 큰 공백"이라 스스로 적어 둔
> 본문이었다. **지도가 있는데 찾지 못하는 상태**가 가장 나쁘다.

---

## 5. 프론트매터 — 최소 규칙

```yaml
---
종류: 신학
상태: 씨앗          # 씨앗 → 발전중 → 확정 (내용 성숙도)
검토: 미검토        # 미검토 → 검토완료 (사람이 확인했는가) ← AI가 못 바꾼다
출처: ["[[2025-01-05 은혜 위에 또 은혜를 받았다]]"]
태그: [설교위키, 신학, 본문/요한복음/1장]
---
```

- **날짜는 항상 `YYYY-MM-DD`.**
- **성경본문은 줄임말 금지**: `출애굽기 3:1-15` (✅) / `출 3:1-15` (❌)
- **프론트매터 안의 위키링크는 큰따옴표**: `"[[링크]]"` / 본문에서는 그냥 `[[링크]]`
- **`상태`와 `검토`는 다른 축이다.** `상태`는 내용이 얼마나 익었는가, `검토`는 사람이 봤는가.
- **`출처`를 비우지 마라.** Wiki에서 원본으로 돌아가는 유일한 선이다. 책·유튜브에서 온 지식은 이것이 유일한 근거다.

### 태그는 통제 어휘다 — 여섯 축만

자유 주제어(`#십자가`, `#기도생활`)는 **쓰지 않는다.** 표기가 갈리고 `22. 설교주제` Wiki와 중복된다.

| 축 | 형식 | 예 |
|---|---|---|
| 종류 | `설교위키`, `{종류}` | `설교위키, 신학` |
| 본문 | `본문/{정식 책 이름}/{장}` | `본문/요한복음/15장` · `본문/시편/63편` |
| 용도 | `용도/{상황}` (10개 고정) | `용도/장례` |
| 절기 | `절기/{표준값}` | `절기/고난주간` |
| 검증 | `검증/{6개 값}` | `검증/출처미확인` |
| 상충 | `상충` | |

- `용도/` 10개: `장례 · 결혼 · 심방 · 새가족 · 다음세대 · 가정 · 재정 · 직장 · 공동체 · 수련회`
- `검증/` 6개: `원어미대조 · 사실미대조 · 출처미확인 · 날짜미상 · 제목미확정 · 원고미완성`
- **절은 태그에 넣지 않는다** (그건 `본문` 필드가 담당).
- **`용도/`는 단어가 아니라 쓸모로 판단한다.** "재정"이라는 낱말이 나온다고 붙이지 않는다. *그 상황에 처한 목회자가 이 노트를 꺼내 쓸 수 있는가*가 기준이다. 보통 한 노트에 0~2개.

#### `본문/` 태그의 개수 — 종류마다 다르다

| 종류 | 상한 |
|---|---|
| `설교주제` · `성경인물` · `신학` (허브 3종) | **없음** |
| 그 밖 전부 | **6개** |

**허브에 상한이 없는 이유와, 대신 품질을 지키는 규칙은 [7장 7-3](#7-3-상한은-품질-장치가-아니라-임의-절단기였다)에 있다.** 요지는 하나다 — **그 장이 노트 본문에 실제로 인용돼 있어야 태그를 붙인다.** 출처 원고가 그 장을 다뤘다는 것만으로는 부족하다.

- ❌ `엘리사` 노트에 `본문/디모데전서/4장` — 엘리사가 나온 설교가 디모데전서 설교였을 뿐이다
- ⭕ `엘리사` 노트에 `본문/디모데전서` — "이 지식은 디모데전서 설교에서 나왔다"는 사실은 맞다

**안전판**: 한 책 안에서 장이 6개를 넘으면 **그 책만** 책 단위로 접는다. 옵시디언 계층 태그는 상위가 하위를 포함하므로 `#본문/사도행전`은 어차피 하위를 전부 잡는다 — **검색에서 잃는 것이 없다.**

### 네 가지 콜아웃 — 태그와 짝으로 쓴다

```markdown
> [!warning] 상충하는 정보     → 태그 `상충` 을 반드시 함께
> [!question] 확인 필요        → 태그 `검증/*` 을 반드시 함께
> [!note] 해석 제안            → AI의 추론임을 밝힌다 (사실처럼 단정 금지)
> [!tip] 핵심 통찰
```

> [!warning] 상충하는 정보
> **콜아웃만 남기고 태그를 빼면 검색되지 않는다.** 콜아웃은 *설명*이고 태그는 *검색*이다.
> 둘 중 하나만 있으면 그 경고는 다시 발견되지 않는다.

---

## 6. AI에게 주는 지침 (복사해서 쓰는 부분)

**아래 전문을 볼트 루트에 `CLAUDE.md`로 저장한다.** Codex·Antigravity를 쓰면 `AGENTS.md`로도 같은 내용을 복사하고, 규칙을 고칠 때 두 파일을 함께 고친다.

---

### 6-1. 목적

목회자의 판단과 고유한 목소리를 대신하지 않는다. 원본과 기존 지식을 근거로 관련 자료를 정확히 찾아 연결하고, 목회자가 선택할 수 있게 돕는다.

### 6-2. 절대 원칙

**원본 보존**

- `10. 원본자료`의 파일은 **덮어쓰거나 축약하지 않는다.**
- 원본을 등록할 때는 **바이트 그대로** 보존한다. 절차는 이렇다:
  1. 원문을 프로그램의 **메모리 변수**로 읽는다 (중간 파일을 거치지 않는다)
  2. `## 원문 또는 원본 메모` 절 아래에 그대로 넣는다
  3. **`assert 원문 in 저장된_파일`로 검증한다**
  4. **검증이 통과한 뒤에만** 수집함 원본을 지운다
- 기존 원문 절을 절대 덮어쓰지 않는다. 다른 버전이 들어오면 `## 원문 또는 원본 메모 (다른 버전, {날짜} 추가)`로 **새 절을 만든다.** 판단이 서지 않으면 병합하지 않는다 — **정보를 잃는 쪽보다 중복이 낫다.**

**경로와 삭제**

- **모든 파일 조작에 절대 경로를 쓴다.** 상대 경로 금지.
- 파일 삭제는 **정확한 절대 경로 하나만**. **글롭(`*`) 절대 금지.**
- 폴더를 만들거나 옮길 때도 절대 경로를 쓴다.

**출처와 인용**

- 원본에서 가져온 주장·인용·예화에는 `출처`와 위치 정보(페이지·시간)를 남긴다.
- 확인하지 못한 문장을 직접 인용처럼 **지어내지 않는다.** 인용자를 **추측해서 채우지 않는다.** 기억으로 문구를 "정확한 원문"으로 고쳐 쓰지 않는다.
- **다만 원고에 실재하는 인용은 원출처를 대조하지 못했어도 인용문 Wiki로 만든다.** 대신 셋을 함께 남긴다:
  1. `검증/출처미확인` 태그 + `> [!question] 확인 필요`
  2. `> [!note] 해석 제안 — 추적 단서` 에 **어디를 찾아보면 되는지**(저작명·강해 번호·판본·역자·검색어). 단서가 없으면 "단서 없음"이라 명시
  3. `## 출처`의 `자료명`을 `미상 — 원고에 출처 표기 없음`처럼 **명시**. 빈칸 금지
- 이 노트는 "확인했다"가 아니라 **"목회자가 확인할 목록에 올렸다"**는 뜻이다. 태그가 붙어 있는 동안 강단에서 원저자 표시 없이 인용하지 않는다.

**저자**

- `저자_발행자`를 **추측해서 채우지 않는다.** 값은 `{실제 이름}` 또는 `미정` 둘뿐이고, **빈 값은 쓰지 않는다** — 빈 값은 "안 채웠다"와 "확인했지만 모른다"를 구분하지 못한다.
- **내 것이 아니라면 그 사람의 이름을 저자로 기록한다.** `미정`에서 멈추지 말고 원창작자를 찾는다.
- **AI로 요약·번역·전사한 것일 뿐이면 원창작자의 이름을 쓴다.** ChatGPT·Gemini 같은 도구는 저자가 아니다. `저자_발행자`에 AI 모델 이름을 적지 않는다.
- 따라서 `미정`은 **최후의 값**이다. 확인 경로는 대개 원고 안에 있다 — 반복되는 원저자 언급, 출처 URL, 유튜브 채널명, 요약 서비스 URL의 슬러그.
- 원창작자가 목회자 본인이 아니면 **폴더도 다시 본다.** `기존자료`는 본인이 완성한 자료를 두는 곳이므로, 외부 자료로 확정되면 재분류 여부를 `확인 필요`에 남기고 **이동은 목회자 확인 후에** 한다.
- 외부 저자에게서 뽑은 예화·통찰·인용은 **Wiki 본문에도 원저자를 적는다** (`출처` 배열만으로는 그 노트를 열어야 알 수 있다).

**지식 위생**

- 기존 Wiki는 내용을 확인한 뒤 병합한다. 동의어 노트를 무분별하게 늘리지 않는다.
- 한 Wiki 노트는 가능한 한 **한 개념만** 다룬다.
- MOC는 관련 Wiki가 5개 이상 모이거나 반복 질문이 생길 때만 만든다.
- `/설교연결`에서 목회자의 1차설교를 **직접 수정하지 않는다.**
- `/활용` 결과를 `20. 설교위키`에 재수집하지 않는다.

**사람이 결정하는 것**

- `검토: 검토완료`는 **목회자만** 설정한다. AI는 항상 `미검토`로 둔다.
- **`검증/*`과 `상충` 태그는 AI가 지우지 않는다.** 목회자가 대조를 마친 뒤 지운다. 이 두 축은 사실상 **목회자의 작업 목록**이다.
- 추론은 단정하지 않고 `> [!note] 해석 제안` 또는 `> [!question] 확인 필요`로 표시한다.

**사람 보호**

- 개인·상담 사례는 **익명화**한다. 성도 실명은 **원본에만** 보존하고 Wiki로 넘기지 않는다.
- **목회적 안전을 검사한다.** 고난을 의미화해 유가족·환자에게 상처가 될 수 있는 논지, 감정 억압으로 오독될 수 있는 논지, 구원 확신을 흔들 수 있는 논지에는 `상충` 태그와 경고를 붙인다. 근거 없이 `용도/장례` 같은 태그를 붙이지 않는다.

### 6-3. 작업 전 필수 확인

1. 요청을 한 문장의 **검색 질문**으로 정리한다.
2. 해당 명령 파일과 템플릿을 읽는다.
3. **같은 제목이나 같은 내용의 노트가 이미 있는지 검색한다.**
4. 원본과 Wiki를 구분한다.
5. 새로 만들 파일과 고칠 파일을 **먼저 알린다.**

### 6-4. 중복 판별 — 이것이 가장 자주 하는 일이다

과거 자료를 대량 등록하면 **작업의 절반이 중복 판별**이다. 부분 일치는 넷 중 하나다. 원고를 **끝까지 읽고** 근거를 대어 판정한다.

| 정체 | 어떻게 처리하는가 |
|---|---|
| **재수출본** (같은 원고를 다시 내보낸 것) | 기존 노트의 `## 처리 기록`에 재유입 사실만 남긴다. 원문을 또 넣지 않는다 |
| **같은 원고의 다른 버전** | 기존 원문은 그대로, `## 원문 또는 원본 메모 (다른 버전, {날짜} 추가)` 절 신설 |
| **합본 원고의 한 조각** | 몇 번째 조각인지 밝히고 기존 노트에 기록. 별도 등록하지 않는다 |
| **애초에 다른 설교** | 정상 신규 등록 |

**재수출본의 정상적인 차이**: `<br>`이 빈 줄로 풀리고, `==하이라이트==`가 평문이 되고, 이스케이프(`\`)가 사라진다. 이것만 다르면 같은 원고다. 대조할 때 이들을 먼저 정규화한다.

### 6-5. AI 층 판별 — 지어낸 내용을 걸러낸다

과거 자료에는 AI가 후처리한 층이 섞여 들어온다.

| 층 | 흔적 | 위험도 |
|---|---|---|
| **AI 요약층** | 원고 앞에 `> [!summary]` 류 콜아웃 | 낮음 (분리하기 쉽다) |
| **AI 확장층** | 짧은 개요가 산문으로 부풀려져 있다 | **가장 높음** — 지어낸 내용이 목회자의 논지처럼 보인다 |
| **AI 전사/대본층** | 구어체 전사 | 중간 |

결정적 흔적: `생성 모델: gemini-2.5-flash` · `ChatGPT의 말:` · "정리해 드릴까요?" · "Simmered for 1m 46s"

> [!tip] 핵심 통찰
> **층 간 대조가 가장 강력한 방법이다.** 요약층에만 있고 원문에 없는 예화·인용은
> **AI가 지어낸 것**이므로 Wiki로 만들지 않는다. 실제로 이 방법으로 원고에 없는
> 예화 하나가 Wiki 직전에 걸러졌다.

### 6-6. 검색 순서 (연결자료를 만들 때)

1. 요청에서 본문·주제·인물·신학·핵심 질문을 뽑는다.
2. `30. 설교준비/주석`에 같은 본문의 AI 리서치가 있는지 본다. 있어도 `검토: 미검토`면 **"AI 리서치 — 검증 필요"**로 표시한다.
3. **태그로 후보를 좁힌다** — `본문/{책}/{장}` · `용도/{상황}` · `절기/{표준값}`. 성경본문 Wiki의 `관련*` 배열을 따라 다른 책까지 확장한다. `상충`·`검증/*`은 **강단 사용 전 걸러 표시한다.**
4. 제목·별칭·정확한 문구로 직접 일치를 찾는다.
5. 관련 MOC를 읽어 범위를 넓힌다.
6. 아직 MOC에 없는 관련 노트를 찾는다.
7. 필요할 때만 원본자료로 돌아가 문맥과 출처를 확인한다.
8. 관련도·신뢰도·새로움을 평가해 정리한다.

### 6-7. 기록 파일

- **`log.md`** — append-only. 맨 위에 `## [YYYY-MM-DD] /명령어 | 대상` 형식으로 추가한다. **지우거나 덮어쓰지 않는다.**
- **`index.md`** — 통계와 MOC 목록. `/수집`·`/등록`·`/설교완성`이 갱신한다.
- **대시보드**가 있으면 `index.md`와 **같은 값으로 함께** 고친다. `/상태`·`/점검`은 어긋났는지만 보고하고 고치지 않는다(읽기 전용).
- 개수를 셀 때 `.gitkeep`, 파일명에 "안내"가 든 문서, `종류`/`상태`가 `예시`인 파일을 **제외한다.**

### 6-8. 충돌 시 우선순위

1. 목회자가 이번 요청에서 명시한 요구
2. 현재 완성설교 또는 원본
3. 선택한 출력 레시피
4. 그 출력의 좋은 예시 (**내용은 복사하지 않고** 구조·분량·문체만 참고)
5. 이 공통 지침

### 6-9. 여러 AI를 병렬로 쓸 때 (대량 등록)

원고 수십·수백 편을 등록할 때는 작업자(서브에이전트)를 병렬로 돌리는 것이 실용적이다. **동시 10명 정도가 상한이다.** 다음을 지키지 않으면 서로 망친다.

- **작업자는 `log.md` · `index.md` · 대시보드 · `29. MOC/`를 절대 건드리지 않는다.** 이유를 함께 알려 줘야 지켜진다 — **배치가 끝나기 전에는 어떤 작업자도 정확한 전체 숫자를 알 수 없다.** 총괄자가 마지막에 한 번에 쓴다.
- **임시 파일은 작업자별 고유 이름**을 쓴다(`/tmp/b17-03-body.txt`). 고정 이름을 쓰면 다른 작업자가 덮어쓴다.
- **모든 작업자에게 절대 경로 규칙을 명시한다.**
- 작업자가 API 오류로 죽으면 **볼트 무결성(파일 수·중첩 폴더)을 먼저 확인한 뒤** 이어서 시킨다.
- 총괄자는 마무리에 **실측 · `index.md` · 대시보드 세 값이 일치하는지** 검사한다.

---

## 7. 값을 치르고 배운 것

여기가 이 문서의 핵심이다. 아래는 전부 **실제로 사고가 나거나 틀린 뒤에** 만든 규칙이다.

### 7-1. 상대 경로 하나가 원고를 지웠다

한 작업자가 작업 폴더를 수집함에 둔 채 **상대 경로로 파일을 이동**했다. 엉뚱한 위치에 `00. 수집함/기존자료/10. 원본자료/기존자료`라는 빈 중첩 폴더가 생겼고, 그것을 "수집함 원본"으로 오인해 원고를 지웠다. 휴지통·백업에 없었고 볼트는 git 저장소가 아니었다. **서브에이전트 트랜스크립트에 남은 `cat` 출력에서 1,470바이트를 그대로 복원**해 살렸다.

원인은 작업자가 아니라 **지시문의 구멍**이었다. "정확한 경로만 · 글롭 금지"는 계속 말했지만 **"절대 경로를 쓰라"고는 말하지 않았다.**

> **규칙**: 모든 파일 조작에 절대 경로. 볼트를 **git 저장소로 만들어라** — 이 사고는 `git checkout` 한 줄이면 끝날 일이었다.

### 7-2. 자동 중복 대조는 믿을 수 없다

87편을 정규화 대조해 "완전 중복 24 / 신규 48"을 얻었다. 정규화 규칙을 조금 바꾸자 **"21 / 44"로 흔들렸다.** 그리고 스크립트가 "신규"로 분류한 파일에서 작업자가 **중복을 두 건 찾아냈다.**

> **규칙**: 스크립트 결과로 배치 규모를 예고하지 마라. **믿을 수 있는 판정은 작업자가 직접 열어 대조하는 것뿐이다.** 스크립트는 우선순위를 정하는 데만 쓴다.

### 7-3. 상한은 품질 장치가 아니라 임의 절단기였다

허브 노트(`설교주제`·`성경인물`·`신학`)의 `본문/` 태그에 상한 10개를 두었다. 그러자 `바울`이 상한에 도달해 **사도행전 21·22·27장을 태그로 받지 못했다** — 바울에게 가장 중요한 본문들이다. **상한이 관련도가 아니라 등록 순서로 잘랐다.**

폐지 전에 실측했다. 허브 3종의 장 단위 태그 **348개 전부(100%)가 근거 있었다** — 그 장이 노트 본문에 실제로 인용돼 있었다. 품질은 이미 **근거 규칙**이 지키고 있었고 상한은 아무것도 더하지 않았다.

반면 다른 종류에서 발견된 오염 255건은 **예화 55% · 묵상과통찰 34%**에서 나왔다. 그쪽 6개 상한은 제 일을 하고 있어 **유지했다.**

> [!note] 해석 제안
> 두 대안을 검토하고 기각했다. **책 단위로 접기** — 태그는 줄지만 정밀도를 버린다(책 단위만
> 쓰면 검색 정확도 9%였다). **역방향 배열로 대체** — 허브 124개 중 **66개는 배열이 태그보다
> 적게 덮었다**(성경본문 Wiki가 없는 본문은 배열도 없다).

> **규칙**: 상한을 두기 전에 **그 상한이 실제로 무엇을 막고 있는지 실측하라.** 여기서는 아무것도 막지 않고 정확한 정보만 잘라내고 있었다.

### 7-4. 조건 하나 때문에 폴더가 여덟 배치 동안 비어 있었다

"직접 인용은 **원출처가 확인된 경우에만** 만든다"는 규칙 때문에 `26. 인용문` 폴더가 8차 배치까지 **0건**이었다. 니버의 기도·본회퍼·헬렌 켈러·하위징아가 전부 노트가 되지 못하고 원본 안에만 남았다.

> **규칙**: 확인되지 않은 인용은 어차피 원본에 남아 있다. 노트로 만들지 않으면 **검색되지 않고, 목회자가 확인할 기회 자체가 사라진다.** 노트로 만들면 `검증/출처미확인`으로 검색 가능한 **작업 목록**이 된다 — 위험이 커지는 것이 아니라 **보이게 된다.**

### 7-5. 제목과 크기로 내용을 추정하면 틀린다

총괄자가 반복해서 틀렸고 매번 작업자가 원고를 읽어 기각했다.

| 추정 | 실제 |
|---|---|
| 39KB 파일 = 요한복음 6장 | **마태복음 5장(팔복)** — "산에 올라가"는 마태 5:1 배경이었다 |
| 「인생을 바꾸는 하나의 프롬프트」 = AI 생성물 | **연극 프롬프터를 끌어온 수사적 도입**. 사도행전 19장 설교였다 |
| 25KB "확장본" | **설교 준비 노트 모음** — 첫 항목만 같고 나머지 7편은 다른 설교였다 |
| 31KB = 다층 구조 | 단일 원고 (한글은 바이트가 부풀어 보인다) |

> **규칙**: 배정할 때 추정을 **가설로 명시하고 기각을 허용하라.** "이것은 요한복음 6장이다"가 아니라 "요한복음 6장으로 보이는데 확인하라"로 지시한다.

### 7-6. 작업자가 중간 숫자로 기록 파일을 망쳤다

한 작업자가 배치 도중 `log.md`·`index.md`·대시보드에 **전부 틀린 숫자**를 썼다(원본 100 vs 실제 110, 위키 457 vs 483). 다른 작업자도 성급한 로그를 남겼다.

"건드리지 마라"만으로는 부족했다. **이유를 함께 알려 준 뒤에야** 위반이 멈췄다 — "배치가 끝나기 전에는 어떤 작업자도 정확한 전체 숫자를 알 수 없다."

> **규칙**: 금지에는 이유를 붙인다. 이유 없는 금지는 지켜지지 않는다.

### 7-7. 목회적 안전은 별도로 검사해야 한다

논리적으로 맞는 논지가 강단에서 위험할 수 있다. 실제로 걸러낸 것들:

- 「어려운 일이 좋은 일일 수 있다」가 그 논지를 **아이의 장례에 적용**했다 → 4곳에 `상충`, `용도/장례`는 붙이지 않았다
- 「감사는 구원의 증거」가 볼트의 **"구원은 선물"** 축과 부딪히고, 감사를 못 느끼는 성도의 **구원 확신을 흔들 수 있다** → `상충`
- 「감정은 감정으로」가 **감정 억압으로 오독될 위험** → `상충` + `탄식` 노트와 구분해 쓰도록 명시

> **규칙**: Wiki를 만들 때 "이 문장이 가장 약한 성도에게 어떻게 들리는가"를 한 번 묻는다. 그리고 그 판단을 AI가 혼자 끝내지 않고 `상충`으로 **목회자에게 올린다.**

### 7-8. 같은 단어가 반대로 쓰이는 것을 지도가 잡아낸다

MOC를 만들자 볼트 전체의 모순이 드러났다.

- **`거룩`** — 이사야 6장 축은 **구별됨**, 요한복음 17장 축은 **참여**. 둘 다 성경적이지만 한 설교에서 섞으면 회중이 혼란해진다
- **`자기 확신`** — 사도행전 9장은 **회심을 막는 완고함**, 5장 계열은 **담대함의 근거**
- **오귀속 두 건** — `거룩은 분리가 아니라 참여다`는 실제로 로완 윌리엄스, `겸손은 자신을 덜 생각하는 것이다`는 C.S. 루이스가 아닐 가능성

> **규칙**: 이런 것은 **AI가 정리하지 않는다.** 지도 안에 `> [!warning] 상충하는 정보`로 고정하고 **목회자 결정을 기다린다.** 그 상태에서는 "강단에서 두 정의를 함께 쓰지 않는 편이 안전하다"고 함께 적는다.

---

## 8. v1.3.1 → v1.4.0 변경 요약

| 영역 | 바뀐 것 |
|---|---|
| 태그 | 허브 3종(`설교주제`·`성경인물`·`신학`) **상한 폐지**, 그 밖 6개 유지. **근거 규칙**이 품질을 지킨다. 안전판(한 책 6장 초과 시 그 책만 접기) 신설 |
| 태그 | `용도/` 축(10개) 반영 — 축이 **여섯 개**가 되었다 |
| 인용문 | 원출처 미확인도 **노트로 만든다** (`검증/출처미확인` + 추적 단서 + `자료명` 명시 필수) |
| 저자 | `저자_발행자` 규칙 신설 — **내 것이 아니면 원창작자 이름**, AI는 저자가 아니다, `미정`은 최후의 값, 빈 값 금지 |
| 안전 | **절대 경로 의무화**, 글롭 삭제 금지, **`assert` 검증 후 삭제** 프로토콜 |
| 안전 | 목회적 안전 검사(고난 의미화·감정 억압·구원 확신) 항목 신설 |
| 운영 | **중복·버전 판별 4분류**와 재수출본 정규화 규칙 신설 |
| 운영 | **AI 층 판별**(요약/확장/전사) + 층 간 대조법 신설 |
| 운영 | **병렬 배치 운영 규칙** 신설 (작업자 금지 구역 + 이유, 고유 임시파일, 3자 정합성 검사) |
| 운영 | MOC를 **배치마다** 연결하도록 명시 |
| 문서 | [7장](#7-값을-치르고-배운-것) 신설 — 규칙의 근거와 실패 기록 |

---

## 9. 마지막 주의

- **AI가 만든 성경 인용·책 인용·사실 정보는 반드시 원본과 대조하십시오.**
- `검증/*`이 붙어 있는 노트는 **강단에 서기 전 확인 대상**입니다. AI는 이 태그를 지우지 않습니다.
- `상충`이 붙어 있으면 **그 신학적 긴장이 아직 정리되지 않았다는 뜻**입니다.
- **볼트를 git 저장소로 만드십시오.** 이 시스템에서 유일하게 되돌릴 수 없었던 사고는 버전 관리가 없어서 생겼습니다.
- 이 시스템은 목회자의 판단을 **대신하지 않습니다.** 연결을 제안할 뿐이고, 무엇을 강단에서 말할지는 언제나 목회자가 정합니다.
