---
종류: 안내
---

# 여기서 시작하세요

목회자를 위한 개인 지식 시스템입니다. **설교문을 쌓는 창고가 아니라, 다음 설교에서 다시 꺼내 쓸
작은 지식을 모으는 볼트**입니다. 판단은 사람이 하고, AI는 연결만 합니다.

## 0. 이 볼트가 돌아가려면 필요한 것

| 필요한 것 | 왜 |
|---|---|
| **Obsidian** | 노트를 읽고 쓰는 곳 |
| **Claude Code** | `/수집`·`/설교연결` 같은 명령이 도는 곳 |
| macOS·Linux, 또는 **Windows는 WSL** | 원본 보호 훅이 bash와 `python3`를 씁니다 |

> [!warning] Windows를 쓰신다면
> `.claude/hooks/원본보호.sh`가 **WSL 없이는 동작하지 않습니다.** 볼트 자체는 열리지만
> "원본이 지워지지 않게 막아 주는 검사"가 꺼진 상태가 됩니다. WSL을 설치하시거나,
> 같은 검사를 PowerShell로 옮기셔야 합니다.

## 1. 세 가지만 먼저 하십시오

1. **이 폴더를 Obsidian에서 "폴더를 볼트로 열기"로 엽니다.**
2. **`90. 설정/사용안내/07. 속성 작성 규칙.md`의 `14-1`을 채웁니다** — 목회자 이름과 교회 이름.
   이 두 칸이 비어 있으면 AI가 자료의 저자를 판정하지 못합니다.
3. **`00. 수집함`에 자료를 하나 넣고 Claude Code에서 `/수집`이라고 칩니다.**

`90. 설정/사용안내/01. 10분 시작하기.md`에 더 자세한 첫 걸음이 있습니다.

## 2. 플러그인 — 하나는 들어 있고, 나머지는 자동으로 따라옵니다

이 볼트에는 옵시디언 플러그인 셋이 들어 있습니다. **처음 열면 "커뮤니티 플러그인을
켜시겠습니까"라고 물어봅니다. 켜 주십시오.**

| 플러그인 | 하는 일 | 출처 |
|---|---|---|
| **위키 대시보드** | 볼트에 뭐가 어디에 얼마나 있는지 한 화면에서 봄 | 이 볼트 전용 (스토어에 없음) |
| **BRAT** | 아래 Jclaudian을 설치하고 계속 업데이트해 줌 | TfTHacker |
| **Jclaudian** | 옵시디언 안에서 Claude Code와 대화 | Yishen Tu의 Claudian 포크 |

> [!note] BRAT이 Jclaudian을 자동으로 최신으로 유지합니다
> BRAT 설정에 `smdjoo-stack/claudian`이 등록돼 있어, 볼트를 열면 새 버전이 나왔을 때
> 알아서 따라옵니다. 이 둘은 **데스크톱 전용**이라 아이패드·모바일에서는 동작하지 않습니다.
> 「위키 대시보드」는 모바일에서도 동작합니다.

원하시면 `Recent Files`(최근 연 파일)나 `Terminal`(옵시디언 안에서 터미널) 같은 플러그인을
옵시디언 커뮤니티 스토어에서 직접 더 설치하셔도 됩니다.

## 3. 명령어는 12개입니다

Claude Code에서 `/`를 치면 목록이 나옵니다. 자세한 설명은 `CLAUDE.md`의 명령어 표와
`90. 설정/명령어/00. 명령어 사용법.md`에 있습니다.

가장 많이 쓰는 셋은 이것입니다.

- **`/수집`** — 수집함에 넣어 둔 자료를 원본으로 보존하고 설교위키를 만듭니다
- **`/설교연결`** — 새 본문이 기존 지식과 어떻게 이어지는지 찾아 줍니다 (원고는 고치지 않습니다)
- **`/설교완성`** — 확정한 설교를 보존하고, 거기서 나온 새 지식을 위키로 되돌립니다

## 4. 처음엔 비어 있는 것이 정상입니다

이 볼트에는 **견본 세 개**만 들어 있습니다.

- `20. 설교위키/29. MOC/설교주제/두려움 MOC 예시.md` — 완성된 MOC는 이렇게 생겼습니다
- `30. 설교준비/1차설교/2026-09-11 탄식 한복판의 약속 (샘플).md` — 1차설교 양식
- 각 폴더의 `○○ 안내.md` — 그 폴더에 무엇을 넣는지

> [!tip] 먼저 채워야 할 것은 1층입니다
> `10. 원본자료`에 **과거 설교만** 넣으면 위키가 자기 자신만 되새기게 됩니다.
> 읽은 책·본 영상·블로그를 **주 한 편씩이라도** 넣으십시오. 그래야 `/설교연결`이
> "이 주제로 읽은 책이 뭐였지?"에 답할 수 있습니다.

## 5. 절대 지켜지는 것

- `10. 원본자료`의 파일은 **덮어쓰거나 줄이지 않습니다.**
- 확인하지 못한 문장을 **인용처럼 지어내지 않습니다.**
- Wiki의 `검토: 미검토`를 **AI가 스스로 `검토완료`로 바꾸지 않습니다** — 목회자가 읽은 뒤에만 바뀝니다.
- `검증/*`·`상충` 태그도 **AI는 붙이기만 하고 지우지 않습니다.**

전체 규칙은 `LLM-운영지침.md`에 있습니다.
