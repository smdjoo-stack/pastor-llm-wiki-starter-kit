---
description: 월간 점검표 항목을 자동으로 훑어 우선순위와 함께 보고한다(기본 읽기 전용).
---

사용자가 `/점검`을 입력하면 다음을 따른다.

1. `AGENTS.md`, `90. 설정/명령어/점검.md`, `90. 설정/사용안내/06. 월간 점검표.md`를 읽고 그 규칙을 그대로 따른다. **사용자가 명시적으로 "고쳐줘"라고 하지 않는 한 파일을 수정하지 않는다.**
2. 중요도순 목록으로 보고한다.
3. 사용자가 수정을 승인한 항목만 고치고, 고친 경우 `log.md`에 기록한다.
