---
description: 출력 레시피를 교회의 대상·분량·문체에 맞게 조정한다.
---

사용자가 `/활용설정 <대상>`을 입력하면 다음을 따른다.

1. `AGENTS.md`와 `90. 설정/명령어/활용설정.md`를 읽고 그 규칙을 그대로 따른다. 한꺼번에 많은 질문을 하지 않는다(5~7개 이내).
2. 바뀐 레시피, 바뀐 핵심 규칙, 참고한 좋은 예시를 요약 보고한다.
3. `log.md` 맨 위에 `## [YYYY-MM-DD] /활용설정 | {대상 레시피}` 항목을 추가한다.
