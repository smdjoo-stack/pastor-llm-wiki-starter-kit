# 이 볼트에서 작업하는 방법 (Codex / Antigravity 등 공용)

이 문서는 `CLAUDE.md`의 미러입니다. 실제 규칙의 출처는 `LLM-운영지침.md`와
`90. 설정/` 하위 문서이며, 이 문서는 Codex·Antigravity처럼 `AGENTS.md`를
표준 프로젝트 지침으로 읽는 에이전트를 위한 진입점입니다. **내용이 서로
갈리면 `LLM-운영지침.md`가 항상 우선**하고, 이 규칙을 고칠 때는 `CLAUDE.md`와
이 문서를 함께 고칩니다.

이 저장소는 목회자를 위한 개인 지식 시스템(Obsidian 볼트)입니다. 코드가 아니라
설교 준비를 위한 Markdown 노트를 다룹니다.

## 여러 에이전트 지원

같은 규칙을 세 가지 도구에서 동일하게 실행할 수 있도록 진입점을 나눠 두었습니다.
실제 실행 절차의 단일 출처(SSOT)는 항상 `90. 설정/명령어/*.md`이고, 아래 세
폴더는 전부 그곳을 가리키는 얇은 진입점입니다.

| 도구 | 표준 지침 파일 | 명령 실행 방식 |
|---|---|---|
| Claude Code | `CLAUDE.md` | `.claude/commands/*.md` — 터미널에 `/`로 자동완성 |
| Codex (GPT) | `AGENTS.md` (이 문서, 저장소 루트에서 자동으로 읽음) | `.agents/skills/*/SKILL.md` — 레포 로컬 스킬 |
| Antigravity | `AGENTS.md` (이 문서) | `.agents/workflows/*.md` — 채팅에서 `/이름`으로 실행 |

새 명령을 추가하거나 기존 명령을 고칠 때는 `90. 설정/명령어/{이름}.md`을 고친 뒤,
`.claude/commands/{이름}.md`, `.agents/skills/{이름}/SKILL.md`,
`.agents/workflows/{이름}.md` 세 곳의 진입점 파일도 같은 이름으로 맞춥니다.

## 핵심 흐름

```
수집 → 원본 보존 → Wiki 재사용 → MOC 연결 → 설교 → 활용
```

- `00. 수집함` — **인박스.** 새 자료를 분류하지 않고 일단 넣어두는 곳. **하위 폴더는 구속력이 없다** — 어느 칸에 넣든 `/수집`이 읽고 제자리로 보낸다. 수집함 밖의 특정 파일·폴더만 처리하려면 `/위키`
- `10. 원본자료` — 원문을 그대로 보존 (기존자료, 책, 블로그, 유튜브, 기사와자료, 떠오른생각) — 하위 폴더명은 `00. 수집함`과 항상 같아야 하지만, **실제 분류는 이쪽이다.** `기존자료`는 설교뿐 아니라 강의·성경공부·교육자료 등 목회자가 과거에 완성한 모든 자료를 포괄한다. 넣을 때 칸을 잘못 골랐어도 `/수집`이 옮기고 이유를 원본 노트에 적는다
- `20. 설교위키` — 재사용 가능한 작은 지식 단위 (성경본문/설교주제/성경인물/신학/예화/인용문/묵상과통찰/설교아이디어)
  - `29. MOC` — 여러 노트를 묶는 설교 연구 지도 (성경본문/설교주제/성경인물/신학)
- `30. 설교준비` — 1차설교 → 연결자료 → 완성설교, + 주석(AI 리서치, Wiki화하지 않는 참고자료)
- `50. 질문` — `/질문`으로 만든 질문-답변 기록. 특정 설교와 무관한 자유 질문용 (`연결자료`와 형제 관계)
- `70. 설교활용` — 완성설교에서 파생된 콘텐츠 (소그룹나눔/주보에세이/카드뉴스/숏츠/SNS)
- `80. 참고자료` — 이미지·PDF 등 첨부파일
- `90. 설정` — 명령어, 템플릿, 출력레시피, 출력예시, 사용안내

## 설교 준비의 기본 흐름

```
본문 / 설교 아이디어
      ↓  /설교연결      기존 Wiki 연결 탐색 (본문→설교→개념→논리→예화→원자료→새 연결)
연결자료 생성
      ↓  (대화)          원고 작성 — 명령이 아니다. 문장은 목회자의 것이다
설교 확정
      ↓  /설교완성       보존 + 새 지식을 Wiki로 되돌림 (환원 체크리스트 8항목)
      ↓  /MOC갱신        밀린 연결 일괄 반영
      ↓  /활용           주보·카드뉴스·숏츠 등
```

**새 설교를 만들 때마다 위키도 함께 자라야 한다.** 그러지 않으면 위키는 과거 설교 저장소로 굳고 다음 설교를 돕지 못한다. `/설교완성`의 환원 체크리스트가 그 장치다.

**지식은 세 층이다** — `10. 원본자료`(어디서 얻었는가) → `20. 설교위키`(무엇을 얻었는가) → `30. 설교준비`(어디에 쓸 것인가). 원본은 출처를 보존하고, Wiki는 설교에서 쓸 생각을 보존한다.

> [!tip] 1층부터 채우십시오
> `10. 원본자료`의 **책·블로그·유튜브·기사와자료**가 비어 있으면 `/설교연결`의 6단계(원자료)가
> 작동하지 않습니다. 과거 설교만 넣으면 위키가 자기 자신만 되새기게 됩니다.
> **주 1편씩이라도 읽은 책·본 영상을 `/수집`하십시오.**


## 명령어

| 명령 | 정의 파일 | 하는 일 |
|---|---|---|
| `/수집` | `90. 설정/명령어/수집.md` | **`00. 수집함` 전체**를 원본자료로 보존하고 설교위키를 만들어 MOC 연결 — **인박스를 비운다** |
| `/위키` | `90. 설정/명령어/위키.md` | **지정한 폴더나 파일**만 원본자료로 보존하고 설교위키 생성. **하는 일은 `/수집`과 같고 대상 지정 방식만 다르다** (공통 절차는 `수집.md`) |
| `/주석` | `90. 설정/명령어/주석.md` | 성경 본문 AI 리서치를 설교준비 참고자료로 만듦 — **Wiki 추출·MOC 연결 안 함** |
| `/주석설정` | `90. 설정/명령어/주석설정.md` | `/주석`의 리서치 깊이·범위 레시피 조정 |
| `/설교연결` | `90. 설정/명령어/설교연결.md` | 새 본문·아이디어가 **기존 지식과 어떻게 연결되는지 발견** — 7단계 검색 · 연결 강도 ★5단계 · 🔴충돌 탐지. 초안 수정 금지 |
| `/설교완성` | `90. 설정/명령어/설교완성.md` | 확정 설교를 완성설교로 보존, Wiki/MOC 갱신 |
| `/MOC갱신` | `90. 설정/명령어/MOC갱신.md` | 밀린 MOC 연결을 찾아 일괄 반영 — **MOC를 새로 만들지는 않음** |
| `/질문` | `90. 설정/명령어/질문.md` | 특정 설교와 무관하게 설교위키에 자유 질문을 던져 답을 찾음 — Wiki/MOC 새로 안 만듦 |
| `/활용` | `90. 설정/명령어/활용.md` | 완성설교를 소그룹/주보/카드뉴스/숏츠/SNS 콘텐츠로 변환 |
| `/활용설정` | `90. 설정/명령어/활용설정.md` | 교회 문체·형식에 맞게 출력 레시피 조정 |
| `/상태` | `90. 설정/명령어/상태.md` | 볼트 현황 스냅샷 — **읽기 전용** |
| `/점검` | `90. 설정/명령어/점검.md` | 월간 점검표 항목 자동 점검 — 기본 **읽기 전용** |

명령을 지원하지 않는 방식으로 대화하는 도구(예: 슬래시 커맨드 UI가 없는 GPT 챗)에서는
사용자가 "수집함 처리해줘", "설교완성 실행해줘"처럼 자연어로 요청해도 위 표에서 해당하는
정의 파일을 찾아 같은 절차를 따릅니다.

공통 실행 방식(`90. 설정/명령어/00. 명령어 사용법.md`):
1. `LLM-운영지침.md`와 해당 명령 파일을 읽는다.
2. 대상이 모호하면 파일 목록을 보여주고 짧게 확인한다.
3. 기존 파일 중복을 검색하고 새 파일/수정 파일 계획을 먼저 알린다.
4. 작업 후 생성·수정·보류·확인 필요 항목을 보고한다.
5. 사용자가 "미리보기만" 또는 "파일은 수정하지 마"라고 하면 제안만 하고 실제 파일은 바꾸지 않는다.
6. 파일을 실제로 만들거나 고친 경우(`/상태`, `/점검` 제외) `log.md`에 한 줄을 추가하고,
   필요하면 `index.md`도 갱신한다.

## 기록 파일

- `index.md` — 볼트 전체 통계와 MOC 목록을 담은 마스터 인덱스. `/상태`가 스냅샷을 읽고,
  `/수집`·`/위키`·`/설교완성`이 갱신한다.
- `log.md` — append-only 작업 로그. 각 명령 실행 후 `## [YYYY-MM-DD] /명령어 | 대상`
  형식으로 맨 위에 추가한다. 지우거나 덮어쓰지 않는다.
- `90. 설정/대시보드.html` — `index.md`를 브라우저에서 보기 좋게 보여주는 정적 HTML. 실시간으로
  파일을 읽는 게 아니라 파일 안의 `DATA` 자바스크립트 객체에 값이 박혀 있는 **스냅샷**이다.
  `index.md`가 바뀔 때마다(`/수집`·`/위키`·`/설교완성`) 같은 값으로 `DATA`도 함께 고쳐서
  두 파일이 항상 같은 시점을 가리키게 한다. 개수를 셀 때는 `.gitkeep`, 파일명에 **"안내"** 또는 **"(샘플)"**이 들어간
  문서, `종류`/`상태`가 "예시"인 파일(예: MOC 예시)을 제외한다 — 실제 콘텐츠만 센다.

## 원본 보호 훅 (Claude Code 한정)

`.claude/hooks/원본보호.sh`가 `10. 원본자료/` 아래 파일에 `## 원문 또는 원본 메모` 섹션이
있는지와 내용이 비어 있지 않은지를 쓰기 후 확인하는 검사기는 **현재 Claude Code에서만
동작**합니다. `PostToolUse` 검사이므로 이미 끝난 쓰기를 되돌리는 사전 차단기는 아닙니다.
Codex의 훅은 Bash 도구 호출에만 걸리고 파일 쓰기 도구에는 걸리지 않으며, Antigravity에는
이런 훅 기능이 없습니다. 따라서 Codex·Antigravity로 작업할 때는 아래 절대 원칙을 **훅 없이
스스로 지켜야** 합니다. 어느 도구에서든 Git 버전 관리나 별도 백업을 함께 사용합니다.

Claude Code 검사기는 macOS·Linux의 Bash와 `python3`가 필요합니다. Windows에서는 WSL을
사용하거나 같은 검사를 PowerShell로 바꾸어야 합니다.

파일명에 "안내"가 들어간 문서는 이 훅에서 제외됩니다 — 원본 콘텐츠가 아니라 폴더 설명
문서이기 때문입니다. `주석`은 애초에 `30. 설교준비/`에 저장되어 이 훅의 대상(`10. 원본자료/`)이
아닙니다.

## 절대 원칙 (반드시 준수)

- `10. 원본자료`의 파일은 **덮어쓰거나 축약하지 않는다.**
- 원본에서 가져온 주장·인용·예화에는 **출처**와 가능한 위치 정보(페이지/시간 등)를 남긴다.
- 확인하지 못한 문장을 직접 인용처럼 **지어내지** 않는다. 인용자를 추측해서 채우지 않는다.
  (다만 원고에 **실재하는** 인용은 원출처를 대조하지 못했어도 인용문 Wiki로 만든다 — `검증/출처미확인` 태그 + `확인 필요` 콜아웃 + **추적 단서**를 함께 남긴다. 2026-09-10 완화, `07. 속성 작성 규칙` 15번.)
- 기존 Wiki는 내용을 확인한 뒤 병합하며, 동의어 노트를 무분별하게 늘리지 않는다.
- 한 Wiki 노트는 가능한 한 **한 개념만** 다룬다.
- MOC는 모든 노트에 만들지 않는다 — 관련 Wiki가 5개 이상 모이거나, 반복 등장하는 질문일 때만 제안한다.
- `/설교연결` 단계에서 사용자의 1차설교 파일을 **직접 수정하지 않는다.** 제안은 `연결자료`에만 쓴다.
- `/활용` 결과는 `70. 설교활용`에만 저장하고, **설교위키(`20. 설교위키`)에 재수집하지 않는다.**
- 작업 후에는 새로 만든 파일과 수정한 파일을 사용자에게 요약해서 보고한다.
- 추론은 사실처럼 단정하지 않고 `해석 제안` 또는 `확인 필요`로 표시한다 — 아래 콜아웃 형식 사용.
- 개인·상담 사례는 익명화하고 불필요한 세부 정보를 삭제한다.
- Wiki 노트의 `검토` 필드는 AI가 스스로 `검토완료`로 바꾸지 않는다 — 목회자가 확인한 뒤에만 바뀐다.

## 표시 방법 (Callout)

상충·미확인·핵심 통찰·해석 제안은 문장으로 흘려 쓰지 않고 Obsidian callout으로 표시한다
(전체 정의는 `LLM-운영지침.md` 참고):

```markdown
> [!warning] 상충하는 정보
> [!question] 확인 필요
> [!tip] 핵심 통찰
> [!note] 해석 제안
```

## 검토 상태

`20. 설교위키`의 모든 Wiki/MOC 템플릿에는 `검토: 미검토` 필드가 있다. 새로 만들거나
AI가 내용을 추가한 노트는 기본값 `미검토`로 둔다. 목회자가 직접 확인해야만
`검토완료`로 바뀐다 — "사람이 결정" 원칙을 프론트매터로 추적하는 장치다. `/점검`이
오래 `미검토`로 남은 노트를 찾아 보고한다.

## 속성(프론트매터) 규칙 요약

전체 규칙은 `90. 설정/사용안내/07. 속성 작성 규칙.md`에 있다. 핵심만 요약하면:

- **날짜**: 항상 `YYYY-MM-DD`.
- **성경본문 표기**: `{책 이름} {장}:{절}` (줄임말 금지, 예: `출애굽기 3:1-15`).
- **프론트매터 안 위키링크는 큰따옴표**: `"[[링크]]"` (본문의 `[[링크]]`는 따옴표 없이 그대로).
- **`상태`(내용 성숙도) 값**: Wiki/MOC 8종은 `씨앗 → 발전중 → 확정` 3단계로 통일. `검토`(사람이 확인했는가)와는 다른 축이니 혼동하지 않는다.
- **`절기` 필드**: `1차설교`·`완성설교`에 있다. 대림절/성탄절/사순절/부활절/맥추감사절/추수감사절 등 표준값 목록은 규칙 문서 7번 참고. 해당 없으면 빈 값.
- **설교 연결 필드**: `완성설교`의 `관련본문`·`관련주제`·`관련인물`·`관련신학`·`관련예화`·`관련인용`(위키링크 배열)을 `/설교완성`마다 채운다 — 본문(body)의 서술형 링크는 사람이 읽기 위한 것이고, 이 프론트매터 배열은 나중에 "이 인물/주제로 설교한 모든 완성설교", "이 예화를 전에 썼는지"를 문자열 검색만으로 찾기 위한 것이다. 둘 다 남긴다.
- **`태그` 필드**: 자유 주제어가 아니라 **통제 어휘** 다섯 축이다 — `설교위키`/`{종류}` · `본문/{정식 책 이름}/{장}`(예: `본문/요한복음/15장`, 절은 제외) — **상한은 설교주제·성경인물·신학에는 없고**(허브 노트라 계속 자라는 것이 정상이다. 품질은 "그 장이 노트 본문에 실제로 인용돼 있어야 한다"는 근거 규칙이 지킨다), 그 밖은 6개 · `용도/{장례·결혼·심방·새가족·다음세대·가정·재정·직장·공동체·수련회}` · `절기/{표준값}` · `검증/{원어미대조·사실미대조·출처미확인·날짜미상·제목미확정·원고미완성}` · `상충`. 콜아웃(`확인 필요`/`상충하는 정보`)을 남겼으면 대응 태그를 반드시 함께 붙인다 — 콜아웃은 설명이고 태그는 검색이다. **`검증/*`과 `상충`은 AI가 지우지 않는다**(목회자가 확인 후 지움). 전체 규칙은 `07. 속성 작성 규칙` 13번.
- **성경본문 Wiki의 연결 필드**: `관련본문`·`관련주제`·`관련인물`·`관련신학`·`관련예화`·`관련인용`·`관련통찰` 배열을 채운다(`07. 속성 작성 규칙` 6-1번). 본문(body)의 `## 관련 주제·인물·신학` 절 링크를 그대로 올린 것이며 둘 다 남긴다. 이 배열과 `본문/` 태그가 맞물려야 **본문 ↔ 내용**이 양방향으로 검색된다 — 1차설교를 준비할 때 "이 본문이 무엇과 이어지는가"에 답하는 장치다.
- **`출처` 필드**: `20. 설교위키` 8종 전부에 있다. 이 지식이 나온 **원본자료 노트 또는 완성설교**로의 위키링크 배열이며, `/수집`·`/위키`·`/설교완성`이 Wiki를 만들거나 갱신할 때마다 채운다. 위 "설교 연결 필드"가 `완성설교 → Wiki` 방향이라면 이 필드는 `Wiki → 원본` 역방향이다. 책·유튜브·블로그에서 온 지식은 외부 원본이 유일한 근거이므로 이 필드를 비우지 않는다. `/점검`의 "원본 없는 Wiki" 판정이 이 필드를 본다. 자세한 규칙은 `07. 속성 작성 규칙` 11번.
- **설교 흐름 표**: `index.md`의 "설교 흐름" 표가 완성설교를 날짜순으로 모은 연대기다. `/설교완성`마다 한 행(날짜·제목·본문·절기·시리즈)을 추가한다.
- **`주석`(AI 리서치)**: `30. 설교준비/주석`에 저장하는 독립된 종류 — Wiki로 쪼개지 않는다. 예외적으로 `검토` 필드를 써서 **어디가 대조되지 않았는지**를 표시한다 — `검증/*` 태그가 붙은 항목이 확인 대기 상태다. 리서치 깊이·범위는 `90. 설정/출력레시피/주석.md`(레시피)로 정하고 `/주석설정`으로 조정한다.

## 검색 순서 (연결자료를 만들 때)

1. 1차설교/요청에서 본문·주제·인물·신학·핵심 질문을 추출한다.
2. `30. 설교준비/주석`에 같은 본문의 AI 리서치가 있는지 확인한다. 있으면 참고하되 `검토: 미검토`면 "AI 리서치 — 검증 필요"로 표시한다.
3. **태그로 후보를 좁힌다** — `본문/{책}/{장}` · `용도/{상황}` · `절기/{표준값}`으로 범위를 만들고, 성경본문 Wiki의 `관련*` 배열을 따라 다른 책까지 확장한다. `상충`·`검증/*`은 강단 사용 전 걸러 표시한다.
4. 제목·별칭·정확한 문구로 직접 일치 항목을 찾는다.
5. 관련 MOC를 읽어 탐색 범위를 넓힌다.
6. 전체 설교위키에서 아직 MOC에 없는 관련 노트를 찾는다.
7. 필요한 경우에만 원본자료로 돌아가 문맥과 출처를 확인한다.
8. 관련도·신뢰도·새로움을 평가해 결과를 정리한다.

## 충돌 시 우선순위

1. 사용자가 이번 요청에서 명시한 요구
2. 현재 완성설교 또는 원본
3. 선택한 출력 레시피 (`90. 설정/출력레시피`)
4. 해당 출력의 좋은 예시 (`90. 설정/출력예시`) — 예시의 **내용은 복사하지 않고** 구조·분량·문체만 참고
5. `LLM-운영지침.md`의 공통 운영지침

## 파일 이름 규칙

- 원본자료: `2026-09-13 자료 제목.md`
- 기존자료: `2026-09-13 제목.md` (자료유형은 프론트매터로 구분: 설교/강의/성경공부/교육자료/기타)
- Wiki: 한 노트에 한 개념, 예: `두려움과 통제.md`
- MOC: `두려움 MOC.md`
- 연결자료: `2026-09-13 설교 제목 - 연결자료.md`
- 질문결과: `2026-09-13 Q-{질문 요약}.md`
- 활용 결과: `2026-09-13 설교 제목 - 주보에세이.md`
- 주석: `{본문} 주석.md` (예: `출애굽기 3장 주석.md`)

## 템플릿

새 노트를 만들 때는 `90. 설정/템플릿/`의 해당 템플릿을 사용한다 (1차설교, MOC, 완성설교,
연결자료, 원본자료, 각 Wiki 유형별 템플릿 등).

## 참고 문서 (필요시 직접 열어서 확인)

- `LLM-운영지침.md` — 전체 공통 규칙 원문
- `90. 설정/사용안내/02. 운영 흐름.md` — 전체 흐름도
- `90. 설정/사용안내/03. 안전과 출처 확인.md` — 출처·저작권·개인정보 체크리스트
- `90. 설정/사용안내/07. 속성 작성 규칙.md` — 프론트매터 필드·날짜·성경본문 표기·절기 표준값 전체
- `20. 설교위키/29. MOC/MOC 운영 안내.md` — MOC를 만들 때/만들지 않을 때 기준
- `90. 설정/사용안내/08. 묵상과통찰 색인 - A가 아니라 B.md` — **논리 진입로.** 「A가 아니라 B」 형태의 통찰을 뒤집는 방향으로 묶는 색인
- `90. 설정/사용안내/09. 예화 색인 - 논지별.md` — **예화 진입로.** 예화를 논지 군으로 묶는 색인. 예화는 본문이 아니라 논지로 찾는다
