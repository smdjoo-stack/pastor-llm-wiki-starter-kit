---
description: 밀린 MOC 연결을 찾아 일괄 반영한다. MOC를 새로 만들지는 않는다.
---

# /MOC갱신

## 실행 전 필수로 읽기

1. `LLM-운영지침.md`
2. `90. 설정/명령어/MOC갱신.md` — 미연결 탐지 방법과 판정 기준
3. `20. 설교위키/29. MOC/MOC 운영 안내.md`

위 문서의 규칙을 그대로 따른다. **MOC를 새로 만들지 않는다 — 제안만 한다.**

## 대상

$ARGUMENTS

비어 있으면 전체 MOC를 점검한다.

## 완료 후

- 지도별 추가 건수와 채워진 공백을 표로 보고한다.
- 태그 기반·역방향 미연결 잔여 건수를 보고한다(0이어야 한다).
- `log.md` 맨 위에 추가한다: `## [YYYY-MM-DD] /MOC갱신 | {대상}`
