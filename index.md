---
종류: 인덱스
갱신일: 2026-09-12
---

# 목회자 LLM-Wiki 인덱스

볼트 전체를 한눈에 보기 위한 마스터 인덱스입니다. `/상태`는 실제 개수와 아래 통계의 차이를
읽기 전용으로 보고합니다. `/수집`·`/위키`·`/설교완성`이 새 Wiki/MOC를 만들거나 갱신할 때
아래 통계와 대시보드를 함께 갱신합니다.

같은 내용을 그래프·카드로 보려면 `90. 설정/대시보드.html`을 브라우저로 열어보세요 (파일을 더블클릭). 이 문서가 바뀔 때마다 그 파일도 같은 값으로 함께 갱신됩니다.

## 현황

| 구분 | 개수 |
|---|---|
| 원본자료 | 0 |
| 설교위키 (전체) | 0 |
| MOC | 0 |
| 완성설교 | 0 |
| 수집함 대기 | 0 |

종류별 설교위키 — 성경본문 0 · 설교주제 0 · 성경인물 0 · 신학 0 · 예화 0 · 인용문 0 ·
묵상과통찰 0 · 설교아이디어 0. 검토 상태는 **검토완료 0 / 미검토 0**입니다.

> [!tip] 이 숫자가 움직이기 시작하면 볼트가 사는 것입니다
> `00. 수집함`에 자료를 넣고 `/수집`을 실행하면 원본자료와 설교위키가 함께 늘어납니다.
> `/상태`는 이 표와 실제 개수가 어긋났는지 읽기 전용으로 확인해 줍니다.

## 지식 진입로 (색인)

주제로 찾는 일은 `22. 설교주제`와 MOC가 한다. 아래 둘은 **설교를 쓸 때의 다른 질문**에 답한다.

- [[08. 묵상과통찰 색인 - A가 아니라 B]] — **"지금 회중의 어떤 오해를 뒤집으려 하는가?"** 통찰이 쌓이면 뒤집는 방향 패턴으로 묶인다
- [[09. 예화 색인 - 논지별]] — **"이 논지에 쓸 예화가 있나?"** 예화가 쌓이면 논지 군으로 묶인다. 예화는 본문이 아니라 논지로 찾는다

> [!note] 집계 규칙
> 위 「현황」은 `.gitkeep`, 파일명에 **"안내"**·**"(샘플)"**가 든 문서, `종류`/`상태`가 `예시`인
> 파일을 **제외**한 실제 콘텐츠 수다. `30. 설교준비`의 샘플 3종(1차설교·연결자료·완성설교)은
> 파이프라인 시연용이므로 집계에서 빠진다.

## 설교 흐름 (연대기)

`완성설교`를 설교일 역순(최신 위)으로 모은 표입니다. `/설교완성`이 매번 한 행을 추가합니다.
날짜별로 어떤 본문·절기·시리즈로 설교했는지 한눈에 보기 위한 것입니다.

| 날짜 | 제목 | 본문 | 절기 | 시리즈 |
|---|---|---|---|---|
| | | | | |

## MOC 목록

아직 MOC가 없습니다. **MOC는 관련 Wiki가 5개 이상 모이거나 반복 등장하는 질문이 생겼을 때**
만듭니다 — 처음부터 만들지 않습니다. 기준은 `20. 설교위키/29. MOC/MOC 운영 안내.md`를 보세요.

`29. MOC` 아래 다섯 유형 폴더(성경본문·설교주제·성경인물·신학·절기)는 미리 만들어 두었습니다.
[[두려움 MOC 예시]] 한 편이 **완성된 MOC가 어떤 모습인지** 보여주는 견본으로 들어 있습니다.

## 최근 활동

최신 항목은 `log.md`를 참고하세요.
