# CHANGELOG

목회자 LLM-Wiki 스타트킷의 릴리스 이력입니다. 설계 원칙과 현재 릴리스 신원은 [버전.md](버전.md)를 보십시오.

> 이 계보는 `v1.2.0 → v1.3.0 → v1.3.1 → v1.4.0`입니다. 별도로 배포된 `BC_LLM_WiKI v3.0.x`는
> 구조가 다른 폐기된 계보이므로 이 이력에 포함되지 않습니다 (버전.md의 「버전 계보」 참고).

## v1.4.0 — 2026-09-10

**v1.3.1까지는 구조를 배포했고, v1.4.0은 그 구조로 원고 200편 가까이를 실제로 등록하며 얻은 규칙을 더한 것입니다.** 폴더 구조와 명령은 v1.3.1과 같아 **기존 볼트에 그대로 적용됩니다.**

새 문서 [목회자 LLM-Wiki 스타트킷 v1.4.0.md](목회자%20LLM-Wiki%20스타트킷%20v1.4.0.md) 하나로 전체를 전달할 수 있게 만들었습니다 — 6장이 그대로 `CLAUDE.md`가 되고, 7장이 규칙의 근거입니다.

### 태그 (통제 어휘)

- **허브 3종(`설교주제`·`성경인물`·`신학`)의 `본문/` 태그 상한을 폐지했습니다.** 그 밖은 6개 유지.
  폐지 전 실측에서 허브의 장 단위 태그 **348개 전부(100%)가 근거 있었습니다** — 품질은 상한이
  아니라 **근거 규칙**("그 장이 노트 본문에 실제로 인용돼 있어야 한다")이 지키고 있었고, 상한은
  관련도가 아니라 **등록 순서로** 잘라 내고 있었습니다(`바울`이 사도행전 21·22·27장을 받지 못했습니다).
- **안전판 신설**: 한 책 안에서 장이 6개를 넘으면 **그 책만** 책 단위로 접습니다. 옵시디언 계층
  태그는 상위가 하위를 포함하므로 검색에서 잃는 것이 없습니다.
- `용도/` 축(10개, v1.3.x에서 신설)을 문서 전체에 반영했습니다 — 축이 **여섯 개**가 되었습니다.
  `LLM-운영지침.md`가 아직 "다섯 축"으로 적고 장 단위 표기도 빠져 있던 것을 고쳤습니다.

### 인용문

- **원출처를 대조하지 못한 인용도 인용문 Wiki로 만듭니다.** 이전 규칙("원출처가 확인된 경우에만")
  때문에 `26. 인용문` 폴더가 8차 등록 배치까지 **0건**이었습니다 — 니버의 기도·본회퍼·헬렌 켈러가
  전부 노트가 되지 못하고 원본 안에만 남았습니다.
- 대신 셋을 반드시 함께 남깁니다: `검증/출처미확인` 태그 + `확인 필요` 콜아웃 / `해석 제안 — 추적
  단서`(어디를 찾아보면 되는지, 없으면 "단서 없음" 명시) / `자료명` 명시(빈칸 금지).
- **여전히 금지**: 원고에 없는 문장을 인용처럼 지어내기, 인용자 추측해서 채우기, 기억으로 문구 고치기.

### 저자

- `저자_발행자` 규칙을 신설했습니다. **내 것이 아니라면 그 사람의 이름을 저자로 기록합니다.**
- **AI로 요약·번역·전사한 것일 뿐이면 원창작자의 이름을 씁니다.** ChatGPT·Gemini는 저자가 아닙니다.
- `미정`은 **최후의 값**이고, **빈 값은 쓰지 않습니다** — 빈 값은 "안 채웠다"와 "확인했지만 모른다"를
  구분하지 못합니다.
- 원창작자가 목회자 본인이 아니면 **폴더도 다시 봅니다**(`기존자료` → `유튜브`·`책` 등). 이동은
  목회자 확인 후에 합니다.

### 안전

- **모든 파일 조작에 절대 경로를 씁니다.** 상대 경로 금지.
- **`assert` 검증 후 삭제** 프로토콜: 원문을 메모리로 읽어 저장 → `assert 원문 in 저장된_파일` →
  통과한 뒤에만 원본 삭제. 삭제는 **정확한 절대 경로 하나만, 글롭 금지.**
- **목회적 안전 검사**를 항목으로 넣었습니다 — 고난 의미화(유가족·환자), 감정 억압으로의 오독,
  구원 확신을 흔드는 논지에 `상충` 태그와 경고를 붙입니다.

### 운영

- **중복·버전 판별 4분류**(재수출본 / 다른 버전 / 합본의 한 조각 / 다른 설교)와 재수출본 정규화
  규칙(`<br>` → 빈 줄, `==하이라이트==` 해제, 이스케이프 제거)을 넣었습니다.
- **AI 층 판별**(요약층 / 확장층 / 전사층)과 **층 간 대조법**을 넣었습니다 — 요약층에만 있고 원문에
  없는 예화·인용은 AI가 지어낸 것이므로 Wiki로 만들지 않습니다. **확장층이 가장 위험합니다**(지어낸
  내용이 목회자의 논지처럼 보입니다).
- **병렬 배치 운영 규칙**: 작업자는 `log.md`·`index.md`·대시보드·`29. MOC/`를 건드리지 않습니다.
  **금지에 이유를 붙여야 지켜집니다** — "배치가 끝나기 전에는 어떤 작업자도 정확한 전체 숫자를 알
  수 없다." 임시 파일은 작업자별 고유 이름. 마무리에 **실측·index·대시보드 3자 정합성**을 검사합니다.
- **MOC는 배치마다 연결합니다.** 네 배치를 미뤘더니 한 지도에 미연결 43건이 쌓였고, 그중 하나는 그
  지도가 스스로 "가장 큰 공백"이라 적어 둔 본문이었습니다.

### 문서

- 「값을 치르고 배운 것」(7장) 신설 — 여덟 항목 전부 실제 사고·오판 뒤에 만든 규칙입니다.
- **볼트를 git 저장소로 만들 것을 권고 사항으로 넣었습니다.** 이 시스템에서 유일하게 되돌릴 수 없었던
  사고(원고 유실)는 버전 관리가 없어서 생겼습니다.
- `버전.md`에 「버전 계보」 절을 신설해 스타트킷 릴리스 · 볼트 세대 · 폐기된 `BC_LLM_WiKI v3.0.x`를
  구분했습니다. 이 `CHANGELOG.md`를 분리해 버전.md는 설계 원칙만 담습니다.

## v1.3.1에서 바뀐 것 (2026-09-09)

- **원본 보호 설명 교정**: Claude Code의 `PostToolUse` 훅은 이미 끝난 쓰기를 차단하지
  못하므로 "저장 후 누락 감지기"로 정확히 설명합니다. 섹션 제목뿐 아니라 내용이 비었는지도
  검사하고, 빠져 있던 실행 권한을 복구했습니다. Bash·`python3` 실행 조건과 Git·백업 병행
  원칙도 안내합니다.
- **읽기 전용 규칙 통일**: `/상태`는 파일을 갱신하지 않고 실제 값과 인덱스·대시보드의 차이만
  보고합니다. 갱신 책임은 `/수집`·`/등록`·`/설교완성`에 둡니다.
- **`/점검` 정확도 개선**: 원본 없는 Wiki 검사를 21~28 폴더로 한정하고 MOC·안내·예시를
  제외합니다. 출처 링크의 존재·위치·중복도 확인합니다. 폐기된 `확인필요` 상태 대신
  `[!question] 확인 필요` 콜아웃을 검사하며, 오래된 항목 기준을 수집함 14일·미검토 30일로
  명시합니다.
- **출처 연결 보강**: `/설교완성`이 새 Wiki뿐 아니라 내용이 갱신된 기존 Wiki에도 완성설교를
  `출처`로 중복 없이 추가합니다.
- **AI 주석 안전성 개선**: 미검토 AI 주석을 원문처럼 그대로 인용하지 않고 잠정적 연구 메모로
  요약하며, 외부 원문 대조 전에는 직접 인용이나 확정 사실로 사용하지 않습니다.
- **안내 정합성 수정**: MOC 신규 상태를 `씨앗`으로 통일하고 README에 `/질문`, `50. 질문`,
  `주석` 흐름과 도구별 명령 사용법을 보완했습니다.

## v1.3.0에서 바뀐 것 (2026-09-09)

비설교 자료(책·유튜브·블로그)가 설교 자료보다 불리하던 구조를 고쳤습니다.
근본 원인은 **Wiki에서 원본으로 가는 연결선이 프론트매터에 없던 것**입니다.
`06. 설교 연결 필드`가 `완성설교 → Wiki` 방향을 구조화했지만 그 역방향은 본문 글로만
있었고, 설교 자료는 볼트 안에서 자체 완결되니 티가 나지 않았을 뿐입니다.

- **`출처` 필드 신설**: 설교위키 8종 전부의 프론트매터에 원본자료·완성설교를 가리키는
  위키링크 배열을 둡니다(`07. 속성 작성 규칙` 11번). 이제 "이 책에서 뽑은 지식 전부",
  "이 유튜브에서 나온 예화"를 속성 검색으로 찾습니다. `/점검`의 "원본 없는 Wiki"
  항목은 여태 존재하지 않는 `source` 필드를 검사하라고 지시하고 있어 실행이
  불가능했는데, 이 필드로 판정이 가능해졌습니다.
- **`관련예화`·`관련인용` 추가**: 완성설교의 구조적 연결 필드가 4종에서 6종이 됩니다.
  같은 예화나 인용을 두 번 쓰는 일을 속성 검색으로 막습니다 — 이 둘이 책·유튜브에서
  가장 많이 쌓이는 종류인데 빠져 있었습니다.
- **`## 논지 요약` 섹션**: 원본자료 템플릿에 추가. 책·강연의 논지는 "다음 설교에 재사용할
  지식"이 아니라 그 자료 고유의 것이라 Wiki가 아닌 원본 노트에 남깁니다.
- **`자료유형` 확장**: `팟캐스트`·`강연`·`SNS` 추가. 폴더는 `00. 수집함`·`10. 원본자료`
  미러 규칙을 지키려고 6개로 고정하고, 세분화는 속성으로만 합니다.
- **`묵상과통찰` 출처 구조화**: 빈 불릿 하나였던 `## 출처 원본`에 페이지/영상 시간·확인일을
  둡니다. 책에서 얻은 통찰이 주로 여기 쌓이는데 근거 정보가 가장 부족했습니다.
- **`/점검` 항목 추가**: URL 자료(유튜브·블로그·SNS·팟캐스트)인데 원문 발췌 없이 URL만
  남은 원본을 찾습니다. 링크가 사라지면 근거도 사라집니다.

## v1.2.0에서 추가한 것 (2026-09-08)

- **여러 에이전트 지원**: `CLAUDE.md`(Claude Code) / `AGENTS.md`(Codex·Antigravity 공용) 미러, 실행 커맨드는 `.claude/commands/`·`.agents/skills/`·`.agents/workflows/` 세 진입점으로 제공 — 실제 절차는 모두 `90. 설정/명령어/*.md` 하나를 가리킴
- **`/상태`, `/점검` 명령 추가**: 볼트 현황 스냅샷과 월간 점검표 자동 점검 (둘 다 기본 읽기 전용)
- **원본 보호 훅**: `.claude/hooks/원본보호.sh` — Claude Code에서 `10. 원본자료` 파일에 원문 섹션이 없으면 저장을 차단 (Codex·Antigravity에는 이 기술적 강제가 없음, `AGENTS.md`에 명시)
- **`index.md` + `log.md`**: 볼트 통계·MOC 목록을 담은 마스터 인덱스와 append-only 작업 로그
- **`90. 설정/대시보드.html`**: 인덱스를 그래프·카드로 보는 오프라인 정적 대시보드. `index.md`가 갱신될 때 같은 값으로 함께 갱신됨
- **`90. 설정/사용안내/07. 속성 작성 규칙.md`**: 날짜 형식, 성경본문 표기, `상태`/`검토` 값 도메인, 프론트매터 위키링크 따옴표, `절기` 표준값, 완성설교의 `관련본문`/`관련주제`/`관련인물`/`관련신학` 구조적 연결 필드 등 프론트매터 전체 규칙
- **Wiki/MOC 템플릿에 `검토` 필드 추가**: AI 작성(미검토) vs 목회자 확인(검토완료) 구분
