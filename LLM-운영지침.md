# LLM 운영지침

이 문서는 이 볼트를 다루는 AI가 항상 따라야 할 공통 규칙이다.

## 목적

목회자의 판단과 고유한 목소리를 대신하지 않는다. 원본과 기존 지식을 근거로 관련 자료를 정확히 찾아 연결하고, 목회자가 선택할 수 있게 돕는다.

## 변경 전 필수 확인

1. 사용자의 현재 요청을 한 문장의 **검색 질문(query)** 으로 정리한다.
2. 해당 명령 파일과 필요한 템플릿을 읽는다.
3. 같은 제목이나 같은 내용의 노트가 이미 있는지 검색한다.
4. 원본과 Wiki를 구분한다.

## 절대 원칙

- `10. 원본자료`의 파일은 덮어쓰거나 축약하지 않는다.
- 원본에서 가져온 주장·인용·예화에는 `출처`와 가능한 위치 정보를 남긴다.
- 확인하지 못한 문장을 직접 인용처럼 만들지 않는다.
- 기존 Wiki는 내용을 확인한 뒤 병합하며, 동의어 노트를 무분별하게 늘리지 않는다.
- 한 Wiki 노트는 가능한 한 한 개념만 다룬다.
- MOC는 모든 노트에 만들지 않는다. 여러 지식을 탐색할 큰 질문이 생길 때 만든다.
- 사용자의 1차설교를 `/설교연결` 단계에서 직접 고치지 않는다. 제안은 `연결자료`에 쓴다.
- **자료의 주인은 명령을 가르지 않는다** — `/수집`(수집함 전체)과 `/위키`(지정한 폴더·파일)는 **대상 지정 방식만** 다르고 절차는 같다 (2026-09-12, `/등록` → `/위키` 개명). `00. 수집함`은 인박스이고 그 하위 폴더는 구속력이 없다 — 자료를 읽은 뒤 `10. 원본자료`의 맞는 폴더로 보내고 이유를 적는다. 자료의 주인에 달린 규칙은 **`저자_발행자`에 누구 이름을 적는가** 하나뿐이며, **내 것이 아니면 원창작자의 이름을 쓴다**(AI는 저자가 아니다).
- `/활용` 결과는 `70. 설교활용`에만 저장하며 설교위키에 재수집하지 않는다.
- 새 파일과 수정 파일을 작업 후 사용자에게 요약한다.

## 검색 순서

1. 1차설교 또는 요청에서 본문·주제·인물·신학·핵심 질문을 뽑는다.
2. `30. 설교준비/주석`에 같은 본문의 AI 리서치가 있는지 확인한다. 있으면 참고하되 `검토: 미검토`면 "AI 리서치 — 검증 필요"로 표시한다.
3. **태그로 후보를 좁힌다** — `본문/{책}/{장}` · `용도/{상황}` · `절기/{표준값}`으로 범위를 만들고, 성경본문 Wiki의 `관련*` 배열을 따라 다른 책까지 확장한다. `상충`·`검증/*`은 강단 사용 전 걸러 표시한다.
4. 제목·별칭·정확한 문구로 직접 일치 항목을 찾는다.
5. 관련 MOC를 읽어 탐색 범위를 넓힌다.
6. 전체 설교위키에서 아직 MOC에 없는 관련 노트를 찾는다.
7. 필요한 경우에만 원본자료로 돌아가 문맥과 출처를 확인한다.
8. 관련도·신뢰도·새로움을 평가해 결과를 정리한다.

## 근거 표시

연결자료와 산출물의 작업 메모에는 가능한 한 다음을 남긴다.

- 관련 Wiki: `[[노트 제목]]`
- 관련 MOC: `[[주제 MOC]]`
- 원본 출처: `[[원본 파일 제목]]`
- 관계 설명: 왜 지금의 질문에 도움이 되는지 한 문장

## 표시 방법 (Callout)

Wiki, 연결자료, 완성설교 등에서 다음 상황은 Obsidian callout 형식으로 표시한다. 문장으로만 흘려 쓰지 않고 아래 형식을 사용해 눈에 띄게 한다.

```markdown
> [!warning] 상충하는 정보
> 다른 노트·원본과 충돌하는 내용을 발견했을 때.

> [!question] 확인 필요
> 출처·인용·사실관계 등 아직 확인되지 않은 내용.

> [!tip] 핵심 통찰
> 강조해서 남길 만한 핵심 인사이트.

> [!note] 해석 제안
> AI의 추론이나 해석 제안임을 밝힐 때 (사실처럼 단정하지 않음).
```

## 검토 상태 (사람이 결정)

`20. 설교위키`의 각 노트는 프론트매터에 `검토: 미검토` 또는 `검토: 검토완료`를 남긴다.

- 새로 만들거나 AI가 내용을 추가한 노트는 기본값 `미검토`로 둔다.
- 목회자가 내용을 직접 확인한 뒤에만 `검토완료`로 바꾼다. AI가 스스로 `검토완료`로 바꾸지 않는다.
- `/점검`은 오래 `미검토`로 남은 노트를 찾아 보고한다.

## 속성(프론트매터) 작성

모든 노트의 속성은 `90. 설정/사용안내/07. 속성 작성 규칙.md`을 따른다. 특히:

- 날짜는 항상 `YYYY-MM-DD`.
- 성경본문 표기는 `{책 이름} {장}:{절}` (줄임말 금지).
- 프론트매터 안의 위키링크는 큰따옴표로 감싼다: `"[[링크]]"` (본문의 `[[링크]]`는 따옴표 없이).
- `완성설교`의 `관련본문`·`관련주제`·`관련인물`·`관련신학`·`관련예화`·`관련인용`은 나중에 설교를 검색·분석하기 위한 구조적 링크이므로 `/설교완성`마다 반드시 채운다.
- `20. 설교위키` 8종의 `출처`는 그 지식이 나온 원본자료 노트 또는 완성설교로의 위키링크 배열이다. `/수집`·`/위키`·`/설교완성`이 Wiki를 만들거나 갱신할 때마다 채우고 비워 두지 않는다 — 책·유튜브·블로그에서 온 지식은 이 필드가 유일한 근거 연결선이다.
- `태그`는 자유 필드가 아니라 **통제 어휘**다(`07. 속성 작성 규칙` 13번). 여섯 축만 쓴다 —
  `설교위키`/`{종류}` · `본문/{정식 책 이름}/{장}`(절은 제외) · `용도/{10개 값}` · `절기/{표준값}` ·
  `검증/{6개 값}` · `상충`. `본문/` 태그의 상한은 **설교주제·성경인물·신학에는 없고**(허브 노트라
  계속 자라는 것이 정상이다 — 품질은 "그 장이 노트 본문에 실제로 인용돼 있어야 한다"는 근거
  규칙이 지킨다), 그 밖은 6개다.
  `> [!question] 확인 필요` 콜아웃을 남겼으면 대응하는 `검증/*` 태그를, `> [!warning] 상충하는 정보`를
  남겼으면 `상충` 태그를 **반드시 함께** 붙인다. 콜아웃만 있고 태그가 없으면 검색되지 않는다.
- **`검증/*`과 `상충` 태그는 AI가 스스로 지우지 않는다.** 목회자가 확인한 뒤에만 지운다 —
  `검토` 필드와 같은 원칙이며, 이 두 축은 사실상 목회자의 작업 목록이다.

## 충돌 시 우선순위

1. 사용자가 이번 요청에서 명시한 요구
2. 현재 완성설교 또는 원본
3. 선택한 출력 레시피
4. 해당 출력의 좋은 예시
5. 이 공통 운영지침
